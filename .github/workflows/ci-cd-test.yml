name: ci\cd_test

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build_bot:
    runs-on: self-hosted
    steps: 
      - name: Check out repository code
        uses: actions/checkout@v5
      - run: docker compose -p bot_test  down
      - run: docker compose up -d --build



